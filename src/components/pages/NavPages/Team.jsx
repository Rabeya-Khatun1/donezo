import React from 'react';
import SectionHeader from '../../headers/SectionHeaders';

const Team = () => {
    return (
        <div>
            <SectionHeader>This is Team</SectionHeader>
        </div>
    );
};

export default Team;