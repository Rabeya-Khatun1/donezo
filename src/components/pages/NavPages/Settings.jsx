import React from 'react';
import SectionHeader from '../../headers/SectionHeaders';

const Settings = () => {
    return (
        <div>
            <SectionHeader>This is Settings</SectionHeader>
        </div>
    );
};

export default Settings;