import React from 'react';
import SectionHeader from '../../headers/SectionHeaders';

const Help = () => {
    return (
        <div>
            <SectionHeader>This is Help</SectionHeader>
        </div>
    );
};

export default Help;