import React from 'react';
import SectionHeader from '../../headers/SectionHeaders';

const Calendar = () => {
    return (
        <div>
            <SectionHeader>This is Calendar</SectionHeader>
        </div>
    );
};

export default Calendar;